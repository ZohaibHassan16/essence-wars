2. Releases Tab (Permanent - Recommended! ✅)

The workflow publishes to Releases if you create a git tag:

- GitHub repo → Right sidebar → Releases → Your tagged release appears here
- Much more visible and professional
- Permanent, doesn't expire
- Can add release notes


To create a release:

```bash
git tag v0.7.0
git push origin v0.7.0
```